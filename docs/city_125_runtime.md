# CITY 125 Executable Address Fabric

This implements the platform model as a 125-address computational city.

A cell is not merely a cubie. It stores typed runtime state: source, state/witness, operator, evidence, receipt, provenance, E47 marker, and update time.

The address bijection is pi(x,y,z)=25x+5y+z for x,y,z in {0,...,4}. Cube generators may act as permutations of these addresses. E47/Kartekeya may act on a separately typed 125-dimensional spectral carrier. Sharing an index set does not identify the representations.

CITY-INVARIANT packets require object, operator, invariant, witness, evidence, provenance, next_action. The route table turns next_action into executable instrument routing for the Mini AI Lab cycle.

Evidence stack target:
browser witness -> instrument witness -> repository test -> machine certificate -> AETHERIS receipt -> Citadel/SEE -> human canonical gate.

Current implementation is browser/runtime infrastructure. It does not itself elevate an E2 simulation or external claim to E1/E0.
